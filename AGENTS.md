# 图片 EXIF 项目协作说明（定稿）

## 1. 项目目标

开发一个面向摄影者的轻量级 EXIF 查看工具：用户将本地图片拖入网页或手动选择图片，即可查看主要拍摄参数；如果图片包含 GPS 坐标，可以一键在苹果地图、高德地图、百度地图或谷歌地图中查看拍摄位置。

项目以“操作简单、本地解析、隐私优先、摄影者真正用得上”为核心。

## 2. 可行性研判

### 可以用纯前端 HTML 实现的功能

- 通过拖放区或文件选择器读取本地图片。
- 在浏览器本地解析 EXIF，无需将原图上传到服务器。
- 显示拍摄时间、相机、镜头、焦距、光圈、快门、ISO、曝光补偿、测光模式、闪光灯、白平衡、尺寸、方向和 GPS 等常用数据。
- 将 GPS 的度分秒转为十进制 WGS-84 坐标。
- 根据拍摄地点选择 WGS-84、GCJ-02 或 BD-09，并生成地图链接。
- 作为静态网页本地打开，或部署到 GitHub Pages 等静态网站。

### 不能只靠 HTML 实现的功能

- 普通网页无权向 macOS Finder 的右键菜单添加“显示 EXIF 数据”。
- 如需 Finder 右键入口，必须另外提供 macOS 快捷指令、Automator 快速操作、Finder 扩展或小型原生应用。这与“只有一个免安装 HTML”的要求存在边界冲突。
- 建议将 Finder 右键入口定为第二阶段的可选增强功能，不阻碍网页版先行完成。

### 格式限制

- JPEG 是第一阶段必须稳定支持的格式。
- TIFF 和部分 WebP 可在解析库支持的前提下纳入测试。
- HEIC/HEIF 在不同浏览器中的解码和元数据读取支持不一致，不应在未实测前承诺完整支持。
- Sony ARW 等 RAW 格式不适合作为首版必须功能；可在第二阶段研究专用解析器、WASM 或本地辅助程序。

## 3. 推荐的产品形态

### 第一层：核心网页工具

使用 HTML + CSS + JavaScript 构成，可以本地打开，也可以在线访问。图片数据默认只在用户的浏览器中处理。

### 第二层：macOS 可选快速入口

待网页核心功能稳定后，再评估提供 Finder 快速操作。它可以从右键菜单接收图片，但“如何把文件安全交给网页”需要单独设计，不应依赖网页直接读取未经用户授权的本地路径。

### 跨平台与浏览器兼容目标

核心网页必须同时支持 macOS 和 Windows，不为两种系统分别维护两套程序。

- macOS 必须支持当前主流版本的 Safari、Google Chrome 和 Microsoft Edge。
- Windows 10/11 必须支持当前主流版本的 Microsoft Edge 和 Google Chrome。
- Firefox 列为 macOS 和 Windows 上的正式兼容测试对象，核心的选择文件、拖放、EXIF 解析和地图跳转不应依赖 Chromium 专有 API。
- 兼容基线为各浏览器当前稳定版；在发布前同时抽测上一个主要版本。不承诺支持 Internet Explorer 和已停止支持的旧版 Edge。
- 实现应使用标准的 `File`、`Blob`、`FileReader`/`ArrayBuffer`、`<input type="file">` 和 HTML 拖放 API。
- 使用“功能检测”而不是根据浏览器名称或操作系统猜测能力。
- 同时保留“拖放图片”和“点击选择图片”两种入口，在拖放行为受限时仍可使用文件选择器。
- 项目不应依赖 Chrome/Edge 特有的 File System Access API，也不应依赖固定的本地文件路径。
- 在 GitHub Pages 等 HTTPS 环境下运行是标准发布方式。如需支持直接双击 `index.html` 以 `file://` 方式运行，所有核心资源必须本地化，并必须分别在 Safari、Chrome、Edge 和 Firefox 中实测，避免模块加载、CORS 和浏览器安全策略差异导致失效。
- JPEG 的 EXIF 兼容性是首版硬性要求；HEIC/HEIF 和 RAW 能否解析或预览属于文件格式支持问题，不等同于网页不支持 Windows 或 macOS。

## 4. 首版（MVP）功能范围

1. 单张图片拖放和点击选择。
2. 显示文件名、格式、文件大小、文件最后修改时间、像素尺寸和小尺寸预览图。
3. 显示摄影者常用的主要 EXIF，并提供“展开全部元数据”。
4. 对数值做摄影友好的格式化，例如 `1/250 秒`、`f/8`、`24 mm`、`ISO 100`。
5. 明确区分三种状态：读取成功、图片存在但无 EXIF、文件或格式无法读取。
6. 有 GPS 时显示纬度、经度、海拔（如有）和 GPS 时间（如有），并提供复制坐标按钮。
7. 提供苹果、高德、百度、谷歌四个地图按钮；没有 GPS 时不显示或禁用。
8. 支持退出当前照片（仅清空页面状态，不删除原图），再拖入下一张。
9. 响应式界面，支持 macOS、Windows 桌面浏览器以及 iPhone/iPad 浏览器。
10. 页面显著标注“图片默认不上传，仅在本机浏览器内解析”。

## 5. EXIF 显示优先级

### 主要信息（默认显示）

- 拍摄日期和时间
- 文件最后修改时间（来自文件属性，不是 EXIF 拍摄时间）
- 拍摄者/作者（如有）
- 版权拥有者或版权声明（如有）
- 相机品牌与型号
- 镜头型号
- 实际焦距与 35 mm 等效焦距
- 光圈、快门、ISO
- 曝光补偿、曝光模式、测光模式
- 白平衡、闪光灯状态
- 像素尺寸、图像方向、色彩空间
- GPS 坐标、海拔与方向（如有）

### 详细信息（折叠显示）

- 快门次数（只在文件真实包含且可靠解析时显示）
- 图像描述
- 软件、色彩配置文件、DPI
- 厂商 MakerNote 中能够稳定解析的字段

不得将推测值冒充为图片中真实存在的 EXIF 值。

## 6. GPS 与地图规则

- EXIF GPS 的原始坐标按 WGS-84 处理。
- 使用现有 `WGS-84 to GCJ-02 @MacStudio` 项目中经过测试的大陆转换范围判断和坐标转换逻辑，不重新凭空编写另一套。
- 中国大陆转换范围内：高德使用 GCJ-02，百度使用 BD-09，苹果地图根据中国本地网络/境外或代理网络模式选择坐标。
- 中国大陆转换范围外：保留 WGS-84 原数据，不施加 GCJ-02 或 BD-09 偏移。
- 中国大陆转换范围内，消费版谷歌地图网页使用 GCJ-02；该规则来自天津解放桥等真实地物的代理网络实测，不能把 Google Maps Platform 的全球 WGS-84 接口说明直接套用于消费版中国大陆底图。中国大陆转换范围外仍使用 WGS-84；在中国大陆原始网络环境下能否访问不做保证。
- 地图按钮打开外部网站时，应提示用户 GPS 坐标将被传给所选地图服务。

## 7. 建议的后续扩展

以下功能不默认纳入首版，需在 MVP 稳定后逐项评估：

- 批量拖入多张图片，以表格对比拍摄参数。
- 将主要 EXIF 导出为 CSV 或复制为文本。
- 按相机、镜头、焦距、光圈、ISO 做本地统计。
- 在不上传原图的前提下，使用地图或反向地理编码显示大致地名；该功能会向第三方发送坐标，必须由用户主动触发。
- 元数据隐私检查：提醒用户分享前图片中是否含位置、作者或版权信息。
- EXIF 移除或保留指定字段后导出副本；这涉及重新编码和品质风险，必须单独设计并确保不改动原文件。
- 在地图上显示多张照片的拍摄点。
- 完善 HEIC/HEIF 和 Sony ARW 等 RAW 支持。
- macOS Finder 右键“显示 EXIF 数据”快速操作。

## 8. 隐私、安全与数据原则

- 默认不上传图片，不收集、不保存用户的图片或 EXIF。
- 优先选用可随项目本地部署的解析库，避免核心功能依赖不可控的外部 CDN。
- 小心处理损坏文件、超大文件和恶意元数据；设置合理文件大小上限和异常处理。
- 不覆盖、不修改原图。
- 页面只在用户主动点击地图按钮后向地图服务发送坐标。

## 9. 交互与界面原则

- 首屏只突出一个核心动作：“拖入图片或点击选择”。
- 主要拍摄参数优先，技术性较强的字段放在“全部数据”中。
- 字段名称使用简体中文，必要时在括号内保留标准英文名。
- 不把“无 EXIF”、“不支持的格式”和“文件损坏”混为同一种报错。
- 拍摄时间可能没有时区，应按 EXIF 原值显示，不擅自转换为当前时区。
- 需要按本文件的跨平台浏览器矩阵分别测试，并在 iPhone/iPad 上验证文件选择流程。

## 10. 开发与验证规则

- 开发前先检查本文件、项目原始说明 `图片EXIF项目.md` 和当前工作树。
- `图片EXIF项目.md` 是原始构思记录，未经用户明确要求不删除、不覆盖。
- 引入任何 EXIF 解析库前，检查其许可证、维护状态、文件体积、格式支持和异常文件处理能力。
- 坐标转换优先复用参考项目中已验证的逻辑，但应在本项目中保留清楚的来源说明和测试。
- 每项功能使用真实样本验证，至少包含：有 EXIF 无 GPS、有 EXIF 有境外 GPS、有 EXIF 有中国大陆 GPS、无 EXIF、旋转方向图片、损坏或不支持的文件。
- 发布前至少完成 macOS Safari、macOS Chrome、Windows Edge、Windows Chrome 四项必测，并完成 macOS/Windows Firefox 兼容测试。每项至少验证点击选择、拖放读取、预览、EXIF 展示和地图跳转。
- 涉及网页技术、浏览器 API、地图 URL 或第三方库的现行行为时，在实施前核对官方文档。
- 修改应聚焦当前任务，不在未经确认时同时扩大功能范围或重做界面。
- 每次修改后检查实际差异，并运行与风险匹配的测试。

## 11. 项目名称

### 正式显示名称：影迹·EXIF

“影迹”同时表达影像、拍摄轨迹和 GPS 位置；`EXIF` 直接说明工具用途。中圆点将中文名称和英文缩写连成一个整体，比空格更有识别度，比短横线柔和，更适合摄影工具。

### 名称使用规则

- 网页标题、界面标识和对外介绍统一使用“影迹·EXIF”。
- 网址、代码目录、CSS 类名等技术性名称不使用中圆点，可使用 `yingji-exif` 这类纯英文短横线写法。
- 纯文本环境无法正常显示中圆点时，可退化为“影迹 EXIF”。

## 12. 已确认的产品决策

1. 采用“先完成网页版，Finder 右键入口后续另做”的两阶段方案。
2. 首版以 JPEG 为必须稳定支持格式，HEIC/HEIF 和 RAW 按实测结果后续加入。
3. 项目正式显示名称采用“影迹·EXIF”。
4. 首版只处理单张图片，批量统计和 CSV 导出留到第二阶段。
5. 网页优先做成可以同时本地打开和部署到 GitHub Pages 的静态版本。
6. 核心网页必须同时支持 macOS 和 Windows 的常用主流浏览器，并按本文件的兼容矩阵实测后发布。

---

本文件已于 2026 年 7 月 17 日确认定稿，作为后续开发、验证与发布的协作依据。

## 13. E.O图文外部链接规则

本项目是影迹·EXIF 唯一的功能源码和开发维护地点。

- 元数据解析、格式支持、GPS/地图逻辑、界面交互、测试和版本说明，都应在本项目中维护。
- E.O图文网站仅链接本项目的 GitHub 仓库 `https://github.com/arthurolan/yingji-exif`，不再生成、部署或维护站内镜像。

本规则更新于 2026 年 9 月 2 日，用于明确 E.O图文与本项目之间仅保留外部链接关系。
